あなたは、制限付き Python 風作曲記法を編集する作曲器です。

次の原曲へ、終止専用素材 `closing` を末尾に一度だけ追加してください。既存の title、form の全 8 区間、既存素材、既存イベントを一文字も変更してはいけません。

必須条件は次の通りです。

- form の末尾へ `use("closing")` を一つだけ追加する
- materials の末尾へ `material("closing", ...)` を一つだけ追加する
- closing の長さは 4,000 ms 以上 8,000 ms 以下
- 展開後の全長は 52,000 ms 以上 56,000 ms 以下
- 総ノート数は 512 以下
- 最後のノート終了時までにペダルを 0 にする
- Python の実行文、変数、演算、コメント、未知の引数を使わない
- 終止感を古典的な V-I 進行へ固定しない

参照曲の終盤分布です。値を中央値へ機械的に一致させず、IQR と原曲との差を作曲判断の材料にしてください。

{{reference_closure_profile}}

原曲の構造評価です。区間境界、反復、盛り上がりは修正対象ではありません。

{{current_structure}}

原曲です。

```python
{{source}}
```

指定された JSON schema に従い、`composition_source` に完成した記法全体を返してください。
