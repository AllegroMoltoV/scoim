前回の終止素材追加は機械検査に失敗しました。失敗理由だけを修正してください。

失敗理由:

{{error}}

前回の出力:

```python
{{source}}
```

原曲:

```python
{{original_source}}
```

原曲の title、既存 form の全 8 区間、既存素材、既存イベントを一文字も変更せず、末尾の `closing` だけを追加してください。指定された JSON schema に従い、`composition_source` に完成した記法全体を返してください。
