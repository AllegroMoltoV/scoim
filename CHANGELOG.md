# 変更履歴

このファイルには、SCoIMの利用者から見える変更を記録します。

## [0.2.0] - 2026-09-17

### 追加

- 楽譜を変えない演奏指示を有限な演奏選択語彙へ変換し、新しい`PerformanceSpec`と`RenderedPerformance`を作る内部経路を追加しました。
- `solo_piano_3m_v2`のフェーズ2から8を内部で縦断し、検証済みのMusicXML、SMF、投影台帳、フェーズ3以降の全モデル試行を一つの生成試行bundleへ保存できるようにしました。
- 保存済みの`solo_piano_3m_v2`生成試行bundleから、外部通信なしで同一のMusicXMLとSMFを再構築できるようにしました。
- 公開`scoim realize`を`solo_piano_3m_v2`のフェーズ2から8へ接続し、新規生成の既定経路にしました。
- 同じ入力、構成ID、試行ID、モデル実行条件の公開`v2`処理を、検証済みの工程から再開できるようにしました。
- `v2`の構成bundleと生成試行bundleを、複写した構成manifestとそのSHA-256で結び、通信なしで来歴を検査できるようにしました。
- モデル呼び出し前にprovider、要求モデル識別子、生成設定を固定し、各応答でrunnerが記録した情報と照合するようにしました。
- 同じ承認済み楽曲台本から生成し、機械検査と人間の試聴に合格した約3分の独奏ピアノ曲を、二つのtakeのSMF、MusicXML、来歴を含む公開音楽例として追加しました。

### 修正

- 各モデル試行へ応答hashと検査結果を保存し、採用記録から合格した試行を通信なしで照合できるようにしました。
- 応答Schemaに不合格のモデル応答を内容検査へ渡して例外になる問題を修正し、有限の内容修正または型付き失敗として扱うようにしました。
- フェーズ4で同じ楽譜生成単位に前景を追加するとき、採用済み音符の占有区間を初回要求から示し、衝突した候補と占有区間を内容修正へ返すようにしました。
- 楽曲台本の場面遷移の説明を、構造上の専用遷移と常に同一視してフェーズ2が停止する問題を修正しました。構造が明示した遷移専用区分からPythonが有効な候補を作り、後段モデルはその候補だけを選びます。
- 楽曲設計と演奏選択の応答SchemaがCodexのStructured Outputsで拒否される問題を修正しました。
- 演奏時間が小数のミリ秒になり、SMF変換で失敗する問題を修正しました。
- 区分境界のペダル解除が、次の楽譜生成単位を出所として記録される問題を修正しました。
- 伴奏マテリアル配置がない楽曲で、空のフェーズ5投影台帳を完成状態として再読込みできない問題を修正しました。

### 変更

- `scoim realize`の新規生成は、`--profile`を省略すると`solo_piano_3m_v2`を使うようにしました。`solo_piano_3m_v1`の新規生成は明示指定で利用できます。保存済み生成試行bundleの再生経路はmanifestから判定します。
- 初版の公開Python操作を`realize`一つに限定し、`scoim`ルートの公開名を`realize`、結果型、Codex用Runner、型付き問題に必要な五つへ整理しました。旧境界と下位処理は下位モジュールへ残します。
- 公開packageの版を`0.2.0`へ更新し、配布検査がwheelのmetadataから版を取得するようにしました。
- branch coverageの公開基準を、重要分岐の回帰を追加した実測93.23%に合わせて93%とし、`pyproject.toml`だけで管理するようにしました。
- 参照SMFのコピー用スクリプトは、個人環境の場所を持たず、呼出し側が入力元を明示するようにしました。
- 公開音楽例のMP3は、音声化に使った音源の再配布条件を証明できないため除外しました。SCoIMが生成したSMF、MusicXML、来歴、人間評価は維持しています。

## [0.1.0] - 2026-09-14

### 追加

- 楽曲台本`0.1.0`のJSON Schemaを追加しました。
- 楽曲台本の構文、参照、区分木、関係を検証するPython APIを追加しました。
- SCoIMのシステム設計と楽曲台本契約を、現在有効な設計の正本として追加しました。
- MITライセンスとコントリビューション案内を追加しました。
- 全資産の挙動回帰とSCoIM中核のcoverage検査を分離した標準テスト手順を追加しました。
- 一つの編成、希望演奏時間、演奏指示をまとめる`performance_setup`を楽曲台本へ追加しました。
- 演奏指示を参照し、区分間の`onset_alignment`と`loudness`を実演奏から比較する型付き検査要件を追加しました。
- 楽曲台本の`check`、`show`、`apply`、`approve`を実行する`scoim` CLIを追加しました。
- 承認済みのピアノ独奏・180秒の台本から、構造を保った`PiecePlan`と投影目標台帳を作るPython APIを追加しました。
- 凍結計画値から対照関係を補い、現行IRで失われる同一素材・異区分長と異素材間の区分変奏を型付きで拒否するようにしました。
- 承認済み台本と凍結した下流応答から、検証済みのMusicXML、180秒SMF、診断を外部通信なしで再現するPython APIを追加しました。
- 自然言語目標ごとの機械検査、成果物hash、実測時間、投影目標の検査結果を、実現結果と診断へ記録するようにしました。
- 承認済み台本、凍結応答、MusicXML、SMF、診断を一つの不変な生成試行bundleへ保存し、bundle単体から外部通信なしで再生するPython APIを追加しました。
- bundleの固定配置、実現器版、全ファイルhashを検査し、来歴不一致を`lineage_mismatch`として返すようにしました。
- 自然言語の提案要求とモデル応答から、検証済みのdraftと成功・失敗の提案記録を作るPython APIを追加しました。
- 公開台本を変更せずStructured Outputsへ接続する、非公開の提案転送Schemaを追加しました。
- Codex CLIの実行物、必要な機能、認証を事前検査し、成功・失敗・タイムアウトの証跡を返す台本提案Runnerを追加しました。
- 自然言語、文書ID、編成、希望演奏時間、モデルを明示してdraftを提案する`scoim propose`コマンドを追加しました。
- 生成試行へ、終端状態、成果物用途、型付き理由を分離した共通結果を追加しました。
- 品質または投影目標が不合格でも、再読込み済みのMusicXML、SMF、診断を診断専用bundleとして保存・再生できるようにしました。
- 成果物を作れない生成試行も、入力と型付きの`terminal.json`を原子的に保存するようにしました。
- 保存済みの凍結応答から生成試行bundleを作る`scoim realize`コマンドを追加しました。
- 承認済み台本から一回のCodex呼出しで下流応答を作り、モデル実行記録とともに生成試行へ保存する経路を追加しました。
- モデル開始後のタイムアウトや不正応答も、凍結応答を捏造せず成果物なしの生成試行として保存するようにしました。
- 検証済みの台本提案記録を`--proposal-record`で生成試行へ含め、通信なしの再生時にも来歴を検査できるようにしました。
- 承認済み台本を、構成、和声、旋律、伴奏、演奏表現の順に検証しながら具体化する段階生成を、`scoim realize --model`の公開経路へ接続しました。
- モデル開始前の事前検査、再開可能な作業状態、モデル開始後のfailure bundleを追加しました。
- 通信なしの固定例と、固定Runnerで段階生成を検査する適合例を追加しました。
- sdistからwheelを再構築し、新規Python 3.13環境へ取得済み依存だけで導入して公開経路を検査する配布テストを追加しました。
- 承認済み楽曲台本、構成bundle、生成試行bundleを種類と版で判定する、新しい`scoim realize`経路を追加しました。
- フェーズ4相当の旋律、フェーズ6相当の楽譜、最終演奏を聴き分ける三つのSMFを生成試行bundleへ追加しました。
- 新しい生成試行bundleへ構成bundleのmanifest hashを保存し、通信なし再生時に来歴を検査するようにしました。
- 場面IDから指定場面と直前・直後の場面を表示する、楽曲台本用Python APIを追加しました。
- 現行の楽曲台本から3つのSMFを生成し、通信なしで再生する固定例を追加しました。

### 変更

- プロジェクトの目的を、個別のSMF生成実験から、LLMと人間が楽曲台本を共同編集するOSS基盤SCoIMへ変更しました。
- READMEを、現在の目的、実装範囲、設計境界、開発手順が分かる内容へ書き直しました。
- 公開release前の楽曲台本`0.1.0`から、演奏時間と演奏指示の平坦なfieldを削除し、`performance_setup`へまとめました。
- Codex CLIアダプターを、台本提案と下流実現の両方に使う構造化Runnerとして整理しました。
- 公開console scriptを`scoim`だけに限定し、旧実験コマンドを配布対象から外しました。
- READMEを初版候補の実際の操作、制約、配布検査が分かる内容へ書き直しました。
- `scoim realize`から旧`--frozen-response`と`--run-dir`を外し、入力種類に応じて構成、実現、通信なし再生を選ぶ形へ変更しました。
- `scoim propose`、`check`、`show`、`apply`、`approve`を現行の楽曲台本へ切り替え、承認結果を`realize`へ直接渡せるようにしました。
- 旧`fixed-aba`を互換性検査用と明記し、公開CLIで旧文書を新しい楽曲台本として推測しないようにしました。

### 修正

- 最終素材の伴奏予算が、共同終止より後の発音禁止位置を使って到達可能と誤判定される問題を修正しました。
- Codex Runnerの応答が不正な場合や応答の確定に失敗した場合に、attemptが非終端のまま残る問題を修正しました。
- 葉区分から参照される空の楽譜素材が、演奏化の途中で未捕捉例外になる問題を修正しました。
- 下流モデルの応答SchemaがOpenAI Structured Outputsで拒否され、実モデル生成を開始できない問題を修正しました。

### 既知の制限

- 公開CLIから最後まで実行できる生成プロファイルは`solo_piano_3m_v1`です。新しい中間表現を使う`solo_piano_3m_v2`はフェーズ6までの内部実証であり、演奏生成と最終bundleは公開CLIへ未接続です。
- リリースへ添付する試聴用SMFは、`v2`の未接続部分を診断用に組み合わせて作った成果です。ハッシュで同一性を確認できますが、`v0.1.0`のリリース内容だけでは再生成できません。
- 試聴用SMFの一部のペダル操作では、内部の出所IDが次の区分を指します。聴感上の問題は確認されていませんが、生成過程の追跡情報としては不正確です。
