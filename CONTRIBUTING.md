# SCoIMへのコントリビューション

SCoIMは正式リリース前の開発段階です。公開APIとCLIは変更される可能性があります。

## 最初に確認する文書

変更前に、次の正本を確認してください。

- [SCoIMシステム設計](docs/design/scoim-system.md)
- [SCoIM楽曲設計データ契約](docs/design/scoim-script.md)
- [設計文書の索引](docs/design/README.md)

`docs/reports/`は調査と実験の履歴であり、現在の仕様の正本ではありません。

## 開発環境

現在の検証環境はWindowsとPython 3.13です。依存関係はプロジェクト内の`.venv`へ導入します。

```powershell
.\scripts\setup.ps1
```

通常の変更後は、公開機能と現在の実行時依存を守る標準テスト、静的検査、公開文書のローカルリンク検査を実行してください。

```powershell
.\scripts\test.ps1
```

未検査の重要な分岐を調べる場合は、同じ標準テストへbranch coverageの計測を追加します。coverageは参考情報であり、率だけを合否条件にはしません。

```powershell
.\scripts\test.ps1 -Coverage
```

配布物を変更した場合とrelease前には、sdistからwheelを作り、新しいPython 3.13環境へインストールする配布検査も実行します。公開既定の`solo_piano_3m_v2`と、明示指定する`solo_piano_3m_v1`互換経路を固定応答で生成し、通信なし再生まで確認します。

```powershell
.\scripts\test-package.ps1
```

過去の研究や実験に固有のテストは標準テストへ含めません。再実行する場合は、必要なローカルデータを確認したうえで対象ファイルを明示して実行します。データ不足をskipして標準テストの成功として扱わないでください。

## 変更の進め方

1. 外から見える挙動を1件選び、失敗するテストを追加します。
2. テストを通す最小の実装を行います。
3. 挙動を変えずに重複と命名を見直し、再度テストします。
4. 公開契約や責務が変わる場合は、実装より先に`docs/design/`の正本を更新します。
5. 設計、テスト、実装を1つの機能単位としてコミットします。

コミットメッセージは日本語で、利用者から見た変更内容を書いてください。

## 問題の報告

問題を報告するときは、期待した結果、実際の結果、再現手順、実行環境を含めてください。第三者の非公開データや認証情報は含めないでください。

## ライセンス

本リポジトリへのコントリビューションは、[MIT License](LICENSE)のもとで配布されます。
